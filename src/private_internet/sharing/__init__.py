"""Global sharing — public, tokenised share links for any content item.

A reusable share button in every section (PULSE, SIGNAL, STORIES, ARIA,
HEALTH, FINANCE) mints a public token whose link renders a branded, unfurlable
page viewable by anyone — no account required. Sensitive sections (HEALTH,
FINANCE) expose only a generated highlight card, never raw numbers.
"""
